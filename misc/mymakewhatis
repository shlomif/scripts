#!/usr/bin/env tclsh8.5
#
# Maintain whatis database on my custom man page directories.

set tomake {}

foreach dir [split $env(MANPATH) ":"] {
    if {[file writable $dir]} {
        lappend tomake $dir
    }
}

exec /usr/libexec/makewhatis {*}$tomake
